/**
 * Represents a binary operator within a C program
 */
export enum BinaryOperator {
    MULTIPLY,
    DIVIDE,
    MODULO,
    PLUS,
    SUBTRACT,
    LEFT_SHIFT,
    RIGHT_SHIFT,
    BIT_AND,
    BIT_XOR,
    BIT_OR,
    OR,
    AND,
    EQUAL,
    GREATER_THAN,
    GREATER_THAN_OR_EQUAL,
    LESS_THAN,
    LESS_THAN_OR_EQUAL,
}